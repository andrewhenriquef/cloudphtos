<%= form_with model: @photo do |form| %>
	<div class="form-group">
		<%= form.label :title %>
		<%= form.text_field :title %>
	</div>
	
	<div class="form-group">
		<%= form.label :image %>
		<%= form.file_field :image %>
	</div>

	<%= form.submit %>
<%end%>